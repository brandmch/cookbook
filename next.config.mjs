/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      // Add your image storage domain here (e.g. Cloudinary, S3) in future phases
    ],
  },
};

export default nextConfig;
