// Phase 2: Login form — email + password via NextAuth CredentialsProvider
export default function LoginPage() {
  return (
    <div className="min-h-screen paper flex items-center justify-center p-6">
      <p className="font-hand text-3xl text-ink-soft">Login — coming in Phase 2</p>
    </div>
  );
}
